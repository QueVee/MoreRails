<h1>Review a Restaurant</h1>

<%= form_for @review do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<br/>
	<%= f.label :comment %>
	<%= f.text_area :comment %>
	<br/>
	<%= f.submit 'Submit me now!' %>
<% end %>
